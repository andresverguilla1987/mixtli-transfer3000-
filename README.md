# MixtliTransfer3000 Backend v2.2.2
Auto-normaliza columnas (Teléfono→phone, correo electrónico→email), crea índices únicos parciales, y evita el error 42703.
Start: `node --enable-source-maps server.js` (Node >=20 <23)
